import { test } from './util.mjs';

// import decode from '../lib/decode.mjs';

test('decode: basic', (t) => {
  // const buf = Buffer.from('/test\0\0,iiis\0');
  // t.deepEquals(decode(buf), ['test', 'i', 123], `should be ${buf}`);
  t.done();
});
